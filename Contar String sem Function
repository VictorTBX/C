#include <stdio.h>
#include <stdlib.h>

int main()
{
    char valor[1000];
    int cont = 0, i =0;

    gets(valor);

    while(valor[i] != '\0'){
        cont++;
        i++;
    }

    printf("%d", cont);

    return 0;
}
