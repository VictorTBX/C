#include <stdio.h>
#include <stdlib.h>

int main()
{
    int vetA[10], i;

    for(i =0; i<10; i++){
        printf("Informe o valor a ser inserido: ");
        scanf("%d", &vetA[i]);
    }

    for(i = 9; i>=0; i--){
        printf("%d ", vetA[i]);
    }

    return 0;
}
